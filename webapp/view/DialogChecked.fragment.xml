<core:FragmentDefinition
   	xmlns:mvc="sap.ui.core.mvc"
	xmlns:core="sap.ui.core"
	xmlns="sap.m">

    <Dialog id="dialogChecked" title="{i18n>newBook}">

   <Table id="idCheckedOutBooksTable"
		items="{
			path: '/CheckedSet'
		}"
		growing="true"
		growingThreshold="10">
	
		<columns>
			<Column>
				<Text text="Name" />
			</Column>
			<Column>
				<Text text="Date of checkout" />
			</Column>
			<Column>
				<Text text="Date of return" />
			</Column>
			<Column>
				<Text text="ISBN" />
			</Column>
		</columns>
		<items>
			<ColumnListItem>
				<cells>
					<ObjectIdentifier
						title="{FirstName} {LastName}"
						text="Username: {Username}"/>
					<Text text="{ 
							path: 'CheckoutDate',
							type: 'sap.ui.model.type.DateTime',
							formatOptions: {
    							pattern: 'dd.MM.yyyy'
  							},
  							constraints: {
    							displayFormat: 'Date'
  							}
               			}" 
					/>
					<Text text="{ 
							path: 'ReturnDate',
							type: 'sap.ui.model.type.DateTime',
							formatOptions: {
    							pattern: 'dd.MM.yyyy'
  							},
  							constraints: {
    							displayFormat: 'Date'
  							}
               			}" 
					/>
					<Text text="{Isbn}" />
				</cells>
			</ColumnListItem>
		</items>
	</Table>
   
   </Dialog>
</core:FragmentDefinition>