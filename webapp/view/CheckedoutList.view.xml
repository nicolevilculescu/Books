<mvc:View
	controllerName="org.ubb.books.controller.CheckedoutList"
   	xmlns:mvc="sap.ui.core.mvc"
	xmlns:core="sap.ui.core"
	xmlns="sap.m">

   <Table id="idCheckedOutBooksTable"
		items="{
			path: '/CheckedSet',
			sorter: {
				path: 'CheckoutDate'
			}
		}"
		growing="true"
		growingThreshold="10">
	
		<columns>
			<Column>
				<Text text="Name" />
			</Column>
			<Column>
				<Text text="Date of checkout" />
			</Column>
			<Column>
				<Text text="Date of return" />
			</Column>
			<Column>
				<Text text="ISBN" />
			</Column>
         	<Column>
				<Text text="Title" />
			</Column>
			<Column>
				<Text text="Author" />
			</Column>
		</columns>
		<items>
			<ColumnListItem>
				<cells>
					<ObjectIdentifier
						title="{FirstName} {LastName}"
						text="Username: {Username}"/>
					<Text text="{ 
							path: 'CheckoutDate',
							type: 'sap.ui.model.type.DateTime',
							formatOptions: {
    							pattern: 'dd.MM.yyyy'
  							},
  							constraints: {
    							displayFormat: 'Date'
  							}
               			}" 
					/>
					<Text text="{ 
							path: 'ReturnDate',
							type: 'sap.ui.model.type.DateTime',
							formatOptions: {
    							pattern: 'dd.MM.yyyy'
  							},
  							constraints: {
    							displayFormat: 'Date'
  							}
               			}" 
					/>
					<Text text="{Isbn}" />
					<!-- <Text text="{Title}" />
					<Text text="{Author}" /> -->
				</cells>
			</ColumnListItem>
		</items>
	</Table>
   
</mvc:View>